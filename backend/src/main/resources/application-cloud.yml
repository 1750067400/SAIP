spring:
  datasource:
    url: jdbc:mysql://mysql:3306/saip?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: hhelibeblxyhh123
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.saip.entity

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.saip: DEBUG
